<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>

<h1>Login Form</h1>

{{#failed_attempts}}
    <div class="alert alert-danger">
        Invalid login. You have {{failed_attempts}} failed attempt{{#failed_attempts}}s{{/failed_attempts}}.
    </div>
{{/failed_attempts}}

<!-- Login Form -->
<form method="POST" action="/login">
    <div class="mb-3">
        <label for="username" class="form-label">Username</label>
        <input type="text" class="form-control" id="username" name="username" required>
    </div>
    <div class="mb-3">
        <label for="password" class="form-label">Password</label>
        <input type="password" class="form-control" id="password" name="password" required>
    </div>
    
    <!-- If the form is disabled, you can disable these fields -->
    {{#failed_attempts}}
        <button type="submit" class="btn btn-primary" disabled>Login</button>
    {{/failed_attempts}}
    {{^failed_attempts}}
        <button type="submit" class="btn btn-primary">Login</button>
    {{/failed_attempts}}
</form>

<!-- Link to redirect to the login page -->
<p>If you don't have an account, <a href="/registration-form">register here</a>.</p>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

</body>
</html>
